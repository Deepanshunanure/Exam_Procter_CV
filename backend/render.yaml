services:
  - type: web
    name: proctor-ai-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python proctor_detector.py --server
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18
      - key: PORT
        value: 8765